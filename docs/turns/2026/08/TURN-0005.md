# TURN-0005 — Release hardening and deployment path

- Date: 2026-08-27
- User objective: continue until the project goal is reached, then perform an actual deployment
- Branch: `stage/task-proof-visualizer/turn-0004-public-enforcement`
- Starting revision: `2a6dab262f49c7d4c0abf6d5f78a38825761022f`
- Result revision: `d83a07290b2658c2936d6040c7ff4f60316b6930`
- Checkpoint: `CHK-0005`

## Facts recovered

- PR #4 exact-head Node 20 and Node 22 checks are successful at the starting revision.
- The unscoped `visual-explainer` npm name is owned by the upstream maintainer and this machine has no npm publication session.
- GitHub release publication is available to the repository owner and is the selected alpha channel.
- The previous dependency graph installed non-core optional hosts with high-severity advisories.

## Changes

- added a committed dependency lock and converted CI to `npm ci` with optional peers omitted;
- moved Pi and PPTX host libraries to optional peers and added an explicit PPTX missing-dependency result;
- added a reusable packed-artifact MCP handshake and clean installation smoke test;
- added a tag-triggered verified GitHub prerelease workflow and version/tag binding;
- corrected PR verification to check out the literal head SHA and added a separate exact-head R2 CI reviewer after the Node matrix;
- documented the immutable public artifact installation and Codex MCP registration route;
- synchronized requirements, tasks, traceability, risks, evidence, release, and handoff records.

## Local evidence

- full strict suite: 106 tests, 102 passed, 0 failed, 4 Windows capability skips;
- source verification: 39 JavaScript modules and 5 machine-readable contracts checked;
- checkout MCP contract/handshake: PASS;
- packed artifact: 97 files, clean install PASS, eight-tool handshake PASS;
- production dependency audit with optional peers omitted: 0 vulnerabilities.

## Remote review feedback

- PR #4 run `33075935293` passed the exact-head Node 20 and Node 22 jobs at `93a2704`;
- the separate R2 job ran all 106 Linux tests without skips and passed the checkout MCP handshake, then found that the package smoke test treated the PR ref name `4/merge` as a release tag;
- the repair limits version/tag binding to `GITHUB_REF_TYPE=tag`; a focused regression test, a wrong-tag rejection check, and a simulated PR-ref package run pass locally.
- the repaired exact-head run `33077129005` passed Node 20, Node 22, package/audit reconstruction, and the separate R2 report at `f1f665d`; the R2 verdict is `PASS_WITH_LIMITS` because CI isolation is not a different human/model and unsupported external authority adapters remain capped at `INCONCLUSIVE`.
- the documentation-only head `2515181` again passed Node 20/22 plus the R2 verification and audit, but the report step rejected the progressed authorized release-decision value; the recorder now parses the decision field exactly, accepts the two defined pre-merge authorized states, and has an executable regression test.

## Release and deployment evidence

- final PR #4 head `96afc5f` passed Node 20, Node 22, 107-test Linux R2 reconstruction, clean 97-file package installation, two eight-tool handshakes, and a 0-vulnerability default dependency audit in run `33077809112`;
- PR #4 merged to `main` as `d83a072` without history rewriting;
- tag `v0.11.0-alpha.1` passed release run `33077966508` and published the GitHub prerelease;
- the public asset digest is `sha256:11bb3dc3c5e69acf60128b517c08149ff8b5c8f703c48a93d4c9212be0e8be72`;
- the downloaded asset replaced the npm link with a normal global `0.11.0-alpha.1` install and completed the deployed eight-tool stdio handshake;
- `C:\codex-home\skills\visual-explainer` is installed from the release tag, and Codex lists `visual-explainer-task-proof` as an enabled stdio MCP server pointing to the installed package.

## Boundary

The repository-authority alpha is merged, publicly released, and deployed on this Codex host. This does not add trusted external authority adapters, a different-human/model R3 acceptance, stable-release status, hardware/scientific validation, or observed long-term user outcomes.
