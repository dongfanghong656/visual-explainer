# TURN-0002 — GitHub Actions and strict-review repair

- Date: 2026-08-26
- Repository: `dongfanghong656/visual-explainer`
- Branch: `chat/task-proof-visualizer/v0.1.0-recovery`
- Base revision: `16c640b1fc6873a6d1381c874365a8bb42011946`
- Result revision: `PENDING_SELF_REFERENCE`
- Sync state: PENDING remote commit/readback

## Intent and diagnosis

Repair the real public CI failures without reproducing the connector's prior commit and notification storm. The failed run contained code/test failures; it was distinct from private-repository Actions jobs blocked before runner allocation by account billing or spending controls.

## Changes

- Enforce reviewer role and independent run IDs before review finalization.
- Reject a claim changed after an issued artifact digest and reject findings that cite unknown claim IDs.
- Remove feature-branch `push` triggers so an open PR receives one pull-request workflow instead of duplicate push and pull-request runs.
- Make the workflow visibly execute the strict test finder and real MCP handshake entrypoints.
- Retire the stale legacy test that imported removed modules.
- Skip only host-impossible newline/symlink construction cases on Windows while preserving their Linux execution.

## Evidence and limits

`npm run verify:task-proof` passes locally: 54 tests passed, 0 failed, 4 Windows capability skips; source/JSON checks, the six-tool MCP contract, and a real stdio snapshot handshake passed. This is author-owned local evidence, not independent acceptance. Linux Node 20/22 CI and exact-revision readback remain pending.

## Next action

Atomically push this repair to the managed recovery branch, verify the resulting commit and draft PR checks, then request an independent review before merge or release.
