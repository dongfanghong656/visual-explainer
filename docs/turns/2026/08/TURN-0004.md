# TURN-0004 — Repair, trusted adapters, default-deny MCP, and local deployment

- Date: 2026-08-27
- Repository: `dongfanghong656/visual-explainer`
- Branch: `stage/task-proof-visualizer/turn-0004-public-enforcement`
- Base revision: `598006bc322cb7cb220085c63f46a71a263f4636`
- Result revision: `PENDING_SELF_REFERENCE`
- Checkpoint: `CHK-0004`
- Draft PR: `#4`
- Turn sync: `LOCAL_VERIFIED_PENDING_COMMIT_AND_REMOTE_CI`
- Release state: `NOT_RELEASED`

## User intent

Test, deploy, and improve the Visual Explainer Task Proof project while continuing the existing staged architecture.

## Findings and changes

- repository truth contradicted the prior chat summary: PR #4 Node 20/22 both failed three deterministic migration cases;
- repaired two Protocol 2.4 test fixtures and the stale contract-free Skill/command instructions;
- introduced a server-owned trusted-adapter registry for repository authority, strict-review evidence, named checks, and exact-snapshot lifecycle verification;
- made unclassified future `task_proof_*` MCP tools fail before registration;
- exposed only a non-secret adapter manifest in Review responses;
- added Task Proof package-binary installation guidance;
- diagnosed npm-link startup failure to path aliasing and changed direct-entry detection to compare real paths;
- installed the checkout with `npm link` and verified all three package binaries are registered.

## Verification

- strict suite: 104 tests, 100 passed, 0 failed, 4 Windows capability skips;
- public finalizer focused tests: 9 passed;
- registry/MCP focused tests: 8 passed across current focused files;
- checkout and npm-linked stdio handshakes: PASS with all eight tools and a real snapshot call;
- package dry-run: 94 entries.

## Boundary

This is the claimant/implementation context. Local deployment proves this Windows account can start and call the linked package binary; it is not independent acceptance, npm publication, release, production deployment, or proof for external authority types.

## Next action

Push the reviewed commit, require Node 20 and Node 22 success at the exact PR #4 HEAD, then perform a genuinely distinct R2/R3 review before any merge or publication decision.
