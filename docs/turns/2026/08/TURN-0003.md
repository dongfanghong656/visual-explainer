# TURN-0003 — Frozen contract authority hardening

- Date: 2026-08-27
- Repository: `dongfanghong656/visual-explainer`
- Branch: `stage/task-proof-visualizer/turn-0003-contract-authority`
- Base revision: `f2c0ced20615ffea3c92ddda7bd329dfc056832f`
- Result revision: `PENDING_SELF_REFERENCE`
- Checkpoint: `CHK-0003`
- Draft PR: `#3`
- Turn sync: `SYNCED_AFTER_POST_COMMIT_READBACK`
- Release state: `NOT_RELEASED`

## User intent

Continue adversarial review and improve the Skill/MCP so an implementation AI cannot declare a task complete by selecting an incomplete acceptance set, laundering evidence, inventing a reviewer level, replaying receipts, using same-name weaker checks, or supplying bare passing labels.

## Changes

- upgraded the frozen contract runtime to protocol `2.4.0`;
- added source requirements, source-use completeness, and symmetric criterion mapping;
- froze named-check policy, executable, argument, working-directory, result, reviewer, and Claim-HEAD identity;
- bound Claim identity to contract, criteria, repository, base, and chronology;
- required one trusted-adapter authority receipt for every declared source;
- bound Review identity, procedure, Claim digest, contract, receipt set, exact repository scope, and chronology;
- replaced caller-trusted evidence/lifecycle status strings with verifier-bound assessment objects;
- added a strict orchestration boundary that rejects unknown/extra receipts and context-free verifier outputs;
- distinguished hard tampering failures from missing-evidence inconclusive results;
- added strict schema, example, current specification, adversarial/static/orchestration tests, and synchronized governance records;
- marked protocols 2.1 and 2.3 as superseded.

## Verification

A fresh downloaded branch archive completed dependency installation and `npm run verify:task-proof` with 83 tests passed, 0 failed, and 0 skipped. The final documentation commit is subject to the same exact-head Linux Node 20/22 pull-request checks and post-commit readback before the managing agent reports synchronization.

## Decisions

- claimant-provisional contracts can never yield final `PASS`;
- textual authority levels, assurance labels, standalone reviewer assertions, and caller-supplied passing strings are not authority;
- the final reviewer revalidates every authority source through trusted adapters;
- same-name checks are identified by content-bound policy and verified receipts;
- public integrations must call `computeStrictContractGate`, not the lower-level primitive;
- explicit exclusions and deferrals remain visible and cap source coverage.

## Unresolved

- public MCP/Skill contract enforcement: NOT_IMPLEMENTED;
- trusted external authority adapters: INCOMPLETE;
- distinct R2/R3 review: NOT_ASSESSED;
- merge/tag/release/publication/deployment: NOT_DONE.

## Next action

Start TASK-0007 on a new staging branch only after exact final-head CI/readback is confirmed.
