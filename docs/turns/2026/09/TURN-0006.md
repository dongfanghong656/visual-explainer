# TURN-0006 — Local Codex discovery and activation repair

- Date: 2026-09-04
- User objective: fix why other local Codex tasks do not use the visualizer; web is out of scope
- Branch: `codex/local-codex-integration-repair-v011`
- Starting revision: `b132b432bb411f2e768e3e0e95fe95bc07600e62`
- Result revision: `PENDING_SELF_REFERENCE`
- Checkpoint: `CHK-0006`

## Root cause

- The released deployment registered only the Task Proof server, not the general renderer.
- The active `C:\codex-home\config.toml` was later regenerated and no longer contained the previously recorded Task Proof registration.
- The installed Skills did not declare MCP tool dependencies, so Skill discovery did not bind either Skill to its server.
- The current desktop process cannot hot-load config changes.

## Changes

- added `agents/openai.yaml` dependency metadata to the general and Task Proof Skills;
- added bounded Chinese trigger phrases and retained explicit skip guidance for routine prose/code work;
- added a real three-tool general-renderer handshake and included it in CI, release verification, and packed-artifact smoke;
- documented dual local registration and the restart/new-task activation boundary;
- restored both installed-package server registrations in the active Codex configuration and refreshed the installed Skill metadata.

## Evidence

- both Skill validators pass;
- strict suite: 109 tests, 105 passed, 0 failed, 4 Windows capability skips;
- checkout general renderer: 3-tool handshake PASS;
- checkout and installed Task Proof: 8-tool handshake PASS;
- packed clean installation: 101 files and both server handshakes PASS;
- `codex mcp list/get`: both servers enabled and point to the globally installed `0.11.0-alpha.1` package;
- fresh ephemeral Codex CLI session called `visual_explainer_prepare` and returned the requested topic.

## Boundary

This turn does not change the existing public release asset, enable web integration, prove automatic selection in every task, hot-load the already-running desktop process, or provide an independent R3 review.
