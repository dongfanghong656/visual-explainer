# CHK-0005 — Release candidate hardening

- Date: 2026-08-27
- Branch: `stage/task-proof-visualizer/turn-0004-public-enforcement`
- Base revision: `2a6dab262f49c7d4c0abf6d5f78a38825761022f`
- Result revision: `PENDING_SELF_REFERENCE`
- State: `EXACT_HEAD_NODE_MATRIX_AND_R2_PASS_WITH_LIMITS_PENDING_MERGE`

## Verified changes

- committed `package-lock.json` drives CI and release installation with lifecycle scripts and optional host peers omitted;
- `npm run test:release-package` packs the consumer artifact, installs it in a clean temporary prefix, and calls all eight Task Proof MCP tools;
- the packed PPTX entry starts without its optional peer and reports the missing export dependency explicitly;
- the tag workflow repeats full verification and a production dependency audit before GitHub prerelease creation;
- pull-request jobs explicitly check out the literal reviewed head SHA, and a separate post-matrix CI job records the limited R2 review lane;
- the full local release gate checks 39 modules, 5 contracts, and 106 tests with 102 passed, 0 failed, and 4 Windows capability skips;
- the local default production audit reports zero vulnerabilities with optional peers omitted.

## Remaining gate

The exact-head Node 20/22 matrix and distinct R2 job passed at `f1f665d` in run `33077129005`. The R2 verdict is `PASS_WITH_LIMITS`: CI isolation is not a different human/model, and unsupported external authority adapters remain capped at `INCONCLUSIVE`. `main` merge, tag workflow, public release creation, downloaded-asset installation, and Codex discovery remain pending.
