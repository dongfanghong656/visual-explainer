# TASK-0005 — Clean-install E2E and release decision

- Status: BLOCKED
- Owner: next implementation/reviewer run
- Dependencies: TASK-0001 through TASK-0004
- Write boundary: tests/fixtures needed for MCP integration, defect fixes, evidence/status/release records

## Required work

1. Check out the managed branch in a clean environment.
2. Install the declared Node dependencies without substituting versions silently.
3. Start `visual-explainer-mcp` over stdio with a real MCP client.
4. Invoke all five Task Proof tools against a real temporary Git repository.
5. Verify invalid claim/review calls fail safely and valid render calls produce HTML plus sidecars.
6. Confirm resources/prompts are listed and readable.
7. Run at least one separate reviewer run on a real author claim.
8. Reassess RSK-0001 through RSK-0004 and record the release decision.

## Acceptance criteria

- All applicable commands exit successfully with durable logs.
- Output bundle contents and paths match the tool schemas.
- No generic Visual Explainer regression is introduced.
- Independent reviewer verdict is recorded for the exact candidate commit.
- Release status is explicitly promoted or remains blocked with reason.

## Expected evidence

New EVD IDs for dependency lock/environment, MCP handshake transcript summary, output hashes, negative cases, host/version, and independent reviewer bundle.

## Recovery

Do not call the current candidate user-usable or released until this task passes. Current blocker: RSK-0001.
