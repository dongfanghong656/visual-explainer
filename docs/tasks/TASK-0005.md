# TASK-0005 — Clean-install E2E and release decision

- Status: DONE_WITH_R2_LIMITS
- Owner: release implementation plus distinct reviewer
- Dependencies: TASK-0001 through TASK-0007
- Write boundary: tests/fixtures needed for MCP integration, defect fixes, evidence/status/release records

## Required work

1. Check out the managed branch in a clean environment.
2. Install the lockfile-defined Node dependencies with lifecycle scripts disabled and optional host peers omitted.
3. Start `visual-explainer-mcp` over stdio with a real MCP client.
4. Invoke all eight Task Proof tools against a real Git repository.
5. Verify invalid claim/review calls fail safely and valid render calls produce HTML plus sidecars.
6. Confirm resources/prompts are listed and readable.
7. Run at least one separate reviewer run on a real author claim.
8. Reassess the active release risks, publish a GitHub prerelease artifact, install it without a checkout link, and register the Skill/MCP in Codex.

## Acceptance criteria

- All applicable commands exit successfully with durable logs.
- Output bundle contents and paths match the tool schemas.
- No generic Visual Explainer regression is introduced.
- Independent reviewer verdict is recorded for the exact candidate commit.
- Release status is explicitly promoted or remains blocked with reason.

## Expected evidence

New EVD IDs for dependency lock/environment, MCP handshake transcript summary, negative cases, host/version, public release locator, installed-binary readback, and independent reviewer bundle.

## Completion evidence

- final PR head `96afc5f` passed exact-head Node 20/22 and automated R2 `PASS_WITH_LIMITS` in run `33077809112`;
- PR #4 merged as `d83a072`; tag run `33077966508` published `v0.11.0-alpha.1`;
- the public 97-file asset was digest-checked, installed as a normal global package, and completed the deployed eight-tool handshake;
- the tagged Skill is installed and the Codex MCP server is enabled.

## Recovery

The declared GitHub alpha release and downloaded-artifact readback are complete. Do not reinterpret that as npmjs publication, a stable release, different-human/model R3 acceptance, or support for external authority adapters.
