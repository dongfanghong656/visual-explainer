# TASK-0007 — Enforce Task Contract in all public Author/Reviewer paths

- Status: ALPHA_DEPLOYED_WITH_R2_LIMITS
- Owner: TURN-0004 claimant implementation
- Dependencies: TASK-0006

## Required work

1. Add Task Contract validation and source adapters to the public MCP contract.
2. Require Claim contract binding before rendering or writing artifacts.
3. Require a complete Review, authority receipt set, trusted evidence assessment, named-check receipts, and lifecycle assessment before final acceptance.
4. Update Author and Reviewer Skills/commands so provisional contracts are visibly capped at INCONCLUSIVE.
5. Render contract ID/digest, authority, coverage, reviewer level, and missing gates on the one-screen diagrams.
6. Add negative tests proving every public entrypoint fails closed when contract evidence is absent, stale, weakened, or mismatched.
7. Run an independent R2/R3 review at the exact integrated HEAD.

## Completion boundary

TASK-0006 does not satisfy this task merely because the isolated contract module exists.

## Implemented evidence

- frozen contracts are mandatory in public Claim and Review schemas and handlers;
- Claim, Review, renderer, immutable artifact writer, and strict final gate share one contract-bound path;
- repository authority, strict-review evidence, named-check, and lifecycle verifiers resolve from the server-owned registry;
- unclassified future `task_proof_*` tools fail before MCP registration;
- Author/Reviewer Skill and command instructions use the contract-mandatory sequence;
- local strict suite, real stdio handshake, package dry-run, and npm-linked binary handshake pass.
- final exact-head Node 20/22, automated R2, release-tag workflow, public-download global install, and deployed eight-tool MCP handshake pass.

## Remaining gate

No repository-authority alpha deployment gate remains. Automated R2 does not prove a different-human/model R3 review, and external non-repository authority adapters remain outside this task's implemented subset and capped at `INCONCLUSIVE`.
