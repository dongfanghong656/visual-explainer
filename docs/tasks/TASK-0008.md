# TASK-0008 — Repair local Codex discovery and dual-MCP activation

- Status: DONE_WITH_DESKTOP_RESTART_REQUIRED
- Owner: TURN-0006 claimant implementation
- Dependencies: TASK-0004, TASK-0005

## Required work

1. Determine why other local Codex tasks did not expose or select the visualizer.
2. Add explicit Skill-to-MCP dependency metadata for both server surfaces.
3. Restore both installed-package MCP registrations in the active local Codex configuration.
4. Add a real general-renderer stdio handshake to source, CI, and packed-artifact verification.
5. Add bounded Chinese discovery phrases to both Skills.
6. Demonstrate a live tool call from a fresh Codex session.

## Implemented evidence

- both source Skills validate and declare their matching stdio MCP server;
- the active local configuration lists both installed-package servers as enabled;
- the installed general renderer serves three tools and Task Proof serves eight tools;
- a fresh ephemeral Codex CLI session called `visual_explainer_prepare` and returned the requested topic;
- release verification and clean packed-install smoke cover both MCP surfaces.

## Remaining activation step

The current Codex desktop process was already running before registration and cannot be claimed as hot-loaded. Restart Codex and open a new task to use the repaired configuration in the desktop application.
